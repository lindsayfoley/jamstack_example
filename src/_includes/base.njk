<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Karla&display=swap" rel="stylesheet">
  </head>

  <style>
    body {
      margin: 0;
      font-family: 'Karla', sans-serif;
      text-align: center;
      background-color: #000000;
      color: #ffffff;
    }

    header {
      border-top: 20px solid #009987;
      box-shadow: 0 0.125rem 0.625rem 0 rgba(0,0,0,.08);
      padding: 2rem;
      display: flex;
      font-size: 1.5rem;
    }

    nav {
      padding-left: 1rem;
      text-align: left;
    }

    nav a {
      padding: 0 2rem;
      text-decoration: none;
      font-weight: bold;
    }

    main {
      max-width: 900px;
      margin: 0 auto 4rem;
      padding: 0 2rem;
    }

    h1 {
      font-size: 3.5rem;
    }

    .tip {
      font-weight: bold;
      font-size: 1.3rem;
    }

    li {
      text-align: left;
    }

    a {
      color: #ffffff;
    }

    hr {
      margin: 2.5rem 0;
      border-top: 2px solid #009987;
      border-bottom: 0;
    }

    footer {
      height: 150px;
      background-color: #181818;
      color: #B1B1B1;
      position: relative;
    }

    small {
      position: absolute;
      left: 0;
      right: 0;
      bottom: 2rem;
    }
  </style>

  <body>
    <header>
      😄
      <nav>
        <a href="/">Home</a>
        <a href="/about">About</a>
      </nav>
    </header>
    {{ content | safe }}
    <footer>
      <small>Thanks for visiting! 🙏</small>
    </footer>
  </body>
</html>